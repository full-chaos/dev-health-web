diff --git a/lib/client.js b/lib/client.js
index 9b8b6c4251b1eecac8eed743f2373a496b569373..66f9ee12f7eb291cf06eabd6e56fef0b533a74cf 100644
--- a/lib/client.js
+++ b/lib/client.js
@@ -8,6 +8,31 @@ class ClientFetchError extends AuthError {
 export class ClientSessionError extends AuthError {
 }
 // ------------------------ Internal ------------------------
+// `GET /session` and `POST /session` both rotate the session cookie. A delayed
+// GET can therefore overwrite a newer POST cookie (see
+// https://github.com/nextauthjs/next-auth/issues/8897). Keep the existing
+// focus, storage, and polling reads, but serialize only browser session calls
+// within this tab. SSR and every other client endpoint stay concurrent.
+let browserSessionRequestTail = Promise.resolve();
+function queueBrowserSessionRequest(request) {
+    const previousRequest = browserSessionRequestTail;
+    let release = () => { };
+    browserSessionRequestTail = new Promise((resolve) => {
+        release = () => resolve();
+    });
+    return previousRequest.then(async () => {
+        try {
+            // Do not release until fetch() and response.json() have both completed,
+            // so the browser has applied this response's Set-Cookie first.
+            return await request();
+        }
+        finally {
+            // `request()` turns failed responses into null, and this also covers an
+            // unexpected rejection so a later session request can still proceed.
+            release();
+        }
+    });
+}
 /**
  * If passed 'appContext' via getInitialProps() in _app.js
  * then get the req object from ctx and use that for the
@@ -17,28 +42,33 @@ export class ClientSessionError extends AuthError {
  * @internal
  */
 export async function fetchData(path, __NEXTAUTH, logger, req = {}) {
-    const url = `${apiBaseUrl(__NEXTAUTH)}/${path}`;
-    try {
-        const options = {
-            headers: {
-                "Content-Type": "application/json",
-                ...(req?.headers?.cookie ? { cookie: req.headers.cookie } : {}),
-            },
-        };
-        if (req?.body) {
-            options.body = JSON.stringify(req.body);
-            options.method = "POST";
+    const request = async () => {
+        const url = `${apiBaseUrl(__NEXTAUTH)}/${path}`;
+        try {
+            const options = {
+                headers: {
+                    "Content-Type": "application/json",
+                    ...(req?.headers?.cookie ? { cookie: req.headers.cookie } : {}),
+                },
+            };
+            if (req?.body) {
+                options.body = JSON.stringify(req.body);
+                options.method = "POST";
+            }
+            const res = await fetch(url, options);
+            const data = await res.json();
+            if (!res.ok)
+                throw data;
+            return data;
         }
-        const res = await fetch(url, options);
-        const data = await res.json();
-        if (!res.ok)
-            throw data;
-        return data;
-    }
-    catch (error) {
-        logger.error(new ClientFetchError(error.message, error));
-        return null;
-    }
+        catch (error) {
+            logger.error(new ClientFetchError(error.message, error));
+            return null;
+        }
+    };
+    if (path !== "session" || typeof window === "undefined")
+        return request();
+    return queueBrowserSessionRequest(request);
 }
 /** @internal */
 export function apiBaseUrl(__NEXTAUTH) {
diff --git a/src/lib/client.ts b/src/lib/client.ts
index 22b0f0cbb2aceb5b4d7440ab2486b4721479d286..96521794636e69b386a4d52d969cf3e3b780210e 100644
--- a/src/lib/client.ts
+++ b/src/lib/client.ts
@@ -128,6 +128,33 @@ export interface SessionProviderProps {
 
 // ------------------------ Internal ------------------------
 
+// `GET /session` and `POST /session` both rotate the session cookie. A delayed
+// GET can therefore overwrite a newer POST cookie (see
+// https://github.com/nextauthjs/next-auth/issues/8897). Keep the existing
+// focus, storage, and polling reads, but serialize only browser session calls
+// within this tab. SSR and every other client endpoint stay concurrent.
+let browserSessionRequestTail: Promise<void> = Promise.resolve()
+
+function queueBrowserSessionRequest<T>(request: () => Promise<T | null>) {
+  const previousRequest = browserSessionRequestTail
+  let release = () => {}
+  browserSessionRequestTail = new Promise<void>((resolve) => {
+    release = () => resolve()
+  })
+
+  return previousRequest.then(async () => {
+    try {
+      // Do not release until fetch() and response.json() have both completed,
+      // so the browser has applied this response's Set-Cookie first.
+      return await request()
+    } finally {
+      // `request()` turns failed responses into null, and this also covers an
+      // unexpected rejection so a later session request can still proceed.
+      release()
+    }
+  })
+}
+
 /**
  * If passed 'appContext' via getInitialProps() in _app.js
  * then get the req object from ctx and use that for the
@@ -142,28 +169,33 @@ export async function fetchData<T = any>(
   logger: LoggerInstance,
   req: any = {}
 ): Promise<T | null> {
-  const url = `${apiBaseUrl(__NEXTAUTH)}/${path}`
-  try {
-    const options: RequestInit = {
-      headers: {
-        "Content-Type": "application/json",
-        ...(req?.headers?.cookie ? { cookie: req.headers.cookie } : {}),
-      },
-    }
+  const request = async (): Promise<T | null> => {
+    const url = `${apiBaseUrl(__NEXTAUTH)}/${path}`
+    try {
+      const options: RequestInit = {
+        headers: {
+          "Content-Type": "application/json",
+          ...(req?.headers?.cookie ? { cookie: req.headers.cookie } : {}),
+        },
+      }
 
-    if (req?.body) {
-      options.body = JSON.stringify(req.body)
-      options.method = "POST"
-    }
+      if (req?.body) {
+        options.body = JSON.stringify(req.body)
+        options.method = "POST"
+      }
 
-    const res = await fetch(url, options)
-    const data = await res.json()
-    if (!res.ok) throw data
-    return data
-  } catch (error) {
-    logger.error(new ClientFetchError((error as Error).message, error as any))
-    return null
+      const res = await fetch(url, options)
+      const data = await res.json()
+      if (!res.ok) throw data
+      return data
+    } catch (error) {
+      logger.error(new ClientFetchError((error as Error).message, error as any))
+      return null
+    }
   }
+
+  if (path !== "session" || typeof window === "undefined") return request()
+  return queueBrowserSessionRequest(request)
 }
 
 /** @internal */
