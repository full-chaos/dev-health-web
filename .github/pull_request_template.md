## Summary

<!-- What changed and why? -->

## Testing Evidence (Required)

<!-- Include exact commands run and a short result summary. -->

## UI Risk Notes (Required For UI Changes)

- Risk level: `none` | `low` | `medium` | `high`
- User-facing surfaces touched:
- Screenshots/traces (if applicable):

## Checklist

- [ ] I included testing evidence (commands + outcomes) in this PR body.
- [ ] If `src/**` changed, I either updated tests (`tests/**`, `*.test.*`, `*.spec.*`, `__tests__/`) or added a waiver line.
- [ ] I documented UI risk notes (or marked risk level as `none` when no UI behavior changed).

## Optional Waiver (Only If No Test Files Changed)

Use this exact format in the PR body:

`TEST-WAIVER: <reason tests were not touched>`

