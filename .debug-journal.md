# Debug Journal — CHAOS-2090 screenshot login
Started: 2026-06-04
Goal: Capture local Playwright screenshots after login for CHAOS-2090 pages.

## Environment snapshot (Phase 0)
- Runtime: Node / Next.js dev server through `pnpm exec next dev -p 3102`.
- Entry: `BACKEND_URL=http://api.localhost:3000 NEXT_PUBLIC_DEV_HEALTH_TEST_MODE=true DEV_HEALTH_TEST_MODE=true AUTH_SECRET=... NEXTAUTH_SECRET=... pnpm exec next dev -p 3102`.
- Ports / sockets: app `3102`.
- References read: node.md, playwright-cli.md, 00-setup.md, 02-investigate.md.

## Hypotheses
1. [PENDING] Credential submit failed because auth route returned an error — distinguishing evidence: browser network/console output from sign-in POST — if true, fix is: env/auth.
2. [PENDING] Existing browser state or form selector issue prevented credentials from posting — distinguishing evidence: Playwright network requests and DOM field values — if true, fix is: script selectors.
3. [PENDING] Test mode route guard can be bypassed with a seeded session/cookie or direct app navigation — distinguishing evidence: route response after setting session state — if true, fix is: direct session.

## Failed hypothesis round counter
- Round 1: Playwright login remained on `/auth/signin`; need network/log evidence.

## Artifacts to revert
- [ ] `/tmp/chaos-2090-next-3102.log` — dev-server log. Revert: remove file.
- [ ] Process on port `3102` — dev-server process. Revert: kill listener.
- [ ] `/tmp/chaos-2090-ops-8001.log` — ops API log if worktree API is started. Revert: remove file.
- [ ] Process on port `8001` — ops API process if started. Revert: kill listener.
- [ ] `.debug-journal.md` — local debug journal. Revert: remove file.

## Findings
- Playwright MCP result: `Login stayed on sign-in page: http://127.0.0.1:3102/auth/signin?`.
- Direct NextAuth credentials POST returned `302` and set `authjs.session-token`, confirming credentials and backend auth are valid; dev CSP prevented React form hydration in `next dev`.
