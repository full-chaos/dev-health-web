/**
 * Re-export from shared AuditLogTable (variant="admin").
 * Maintained for backward compatibility.
 */
export { AuditLogTable, type AuditEntry as AuditLog } from "@/components/shared/AuditLogTable";
