query GetMappingCoverageHealth($teamId: ID!) {
    dataHealth(team: $teamId) {
        mappingCoverage {
            deployments {
                totalRepos
                coveredRepos
                coveragePct
            }
            workItems {
                totalRepos
                coveredRepos
                coveragePct
            }
        }
    }
}
