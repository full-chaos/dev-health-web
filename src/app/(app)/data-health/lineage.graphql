query MetricLineage($metricId: ID!) {
  dataHealth(team: "ALL") {
    metricLineage(metricId: $metricId) {
      metricId
      sourceTables
      computeWindow {
        kind
        durationDays
      }
      computedAt
      rowCount
    }
  }
}
